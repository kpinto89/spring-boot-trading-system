spring:
  datasource:
    url: jdbc:h2:mem:trading;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;NON_KEYWORDS=TYPE
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

app:
  jwt:
    issuer: trading-system
    secret: "TEST_SECRET_TEST_SECRET_TEST_SECRET_TEST_SECRET"
    accessTokenMinutes: 120
